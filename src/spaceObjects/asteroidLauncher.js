import Asteroid from "./asteroid.js";
class AsteroidLauncher{
	constructor(pos, game) {
		this.game = game;
		this.pos = pos;
	}
	launchAsteroid(){
		//create function to create new asteroid and yeet it
		//let asteroid = new Asteroid(new Vector2(40, 30), this.pos, this.game);
		//this.game.delObjects.push(asteroid);
	}
}	